language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t umair85/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true umair85/docker-react npm run test
  